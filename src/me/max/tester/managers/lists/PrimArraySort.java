/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package me.max.tester.managers.lists;

import java.util.ArrayList;
import java.util.Arrays;

/**
 *
 * @author Mc170171
 */
public class PrimArraySort {
    
   /* public void primArraySortAsc(int[] array) { // lowest -> higest
        int high = 0;
        for (int item : array) {
            if (item > high) {
                high = item;
            }
        }
      //  ArrayList<Integer> test = new ArrayList<>();
       // ArrayList<Integer> test = Arrays.asList(array);
        System.out.println(test);
        for (int i = 0; i<high+1; i++) {
            if (test.contains(i)) {
                System.out.println(i);
            }
        }
    }
    
    public void primArraySortDes(int[] array) { // highest -> lowest
        int low = 99999;
        for (int item : array) {
            if (item < low) {
                low = item;
            }
        }
        for (int i = 99999; i>low-1; i--) {
            if (Arrays.asList(array).contains(i)) {
                System.out.println(i);
            }
        }
    }
   */ 

}
